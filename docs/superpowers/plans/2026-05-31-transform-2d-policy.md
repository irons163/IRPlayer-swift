# 2D Transform Policy Implementation Plan

**Goal:** Extract 2D transform math from `IRGLTransformController2D` into a pure internal helper and cover it with focused tests.

## Tasks

- [ ] Add failing `IRGLTransform2DPolicyTests`.
- [ ] Implement `IRGLTransform2DPolicy`.
- [ ] Route `IRGLTransformController2D` through policy for update, scroll, degree scroll, and resize decisions.
- [ ] Run focused tests and full simulator suite.
- [ ] Commit each completed step.

